: hex.s ( -- ) base @ >R   hex .s   R> base ! ;

: alloc-typed ( %type length -- value ) cells allocate throw   tuck ! ;

: >var< ( new var -- old ) dup @ -rot ! ;

\ Double exit - this returns from the caller of the caller of BAIL
: bail ( -- ) R> R> 2drop ;

: ?stack-clear ( -- )
  depth ABORT" Stack not empty on init - :noname without method-of etc.?" ;
